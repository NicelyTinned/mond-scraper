# Mond Scraper

A public list of scraped GitHub Mond packages.
Updates every ~40 minutes.

(Do NOT make a PR adding your package to the list. Instead, make sure your package repository fits the criteria below:

# Criteria

1. Has the `mond-package` topic.
2. Has a Mond.toml in the default branch.
